# 精准坐标定位调整（只改席位+手牌坐标，其余保留）

只改 `app/pages/game/[id].vue`。父容器 = 横向椭圆桌可视区域(.table-wrap 或 .table)：宽 100%、高 100%，原点左上 (0,0)。

## 0. 关键前置：去掉页面旋转 hack
- **移除**竖屏时 `transform: rotate(90deg)` 那段媒体查询，前端不做旋转，由用户手动横屏手机适配。首页/房间页保持竖屏原状。

## 一、6 席位精准坐标（沿椭圆外圈，绝对定位到父容器 .table-wrap/.table-inner）
每个 `.seat-N` 用 `position:absolute; transform:translate(-50%,-50%)`，`left`/`top` 用下列中心点：

| 席位 | left | top | 头像区域(参考) |
|---|---|---|---|
| 1 我方 | 30% | 78% | 26–36% × 72–84% |
| 2 右下 | 78% | 70% | 74–84% × 64–76% |
| 3 右上 | 78% | 30% | 74–84% × 24–36% |
| 4 顶右 | 65% | 22% | 61–71% × 16–28% |
| 5 顶左 | 35% | 22% | 31–41% × 16–28% |
| 6 左下 | 22% | 50% | 18–28% × 44–56% |

- 每个头像圆形，尺寸对应约 10% × 12% 区间(具体像素可 clamp 保证不失圆)；
- 头像右上角紫色🎁小图标（.gift-badge）保留；
- 头像下方筹码金黄胶囊（位于席位1的 X:26% Y:85% 等对应位置，通过 seat 内子元素相对定位即可，不需要每个都写死）；
- **非我方 5 席的手牌**：定位到头像**内侧**（靠桌面圆心一侧），牌背+「暂时无法查看」，禁止外溢。

## 二、我方手牌精准坐标（关键：与我方头像分开，不遮挡）
`.me-panel` 独立绝对定位到 .table-wrap 内：
- 中心/起点 `left: 42%, top: 70%`（左上角为准，不 translate）；
- 两张扑克牌横向并排：左牌 X 42%–48%，右牌 X 49%–55%，整体 Y 70%–80%；
- 手牌与我方头像底部(Y≈84%)之间垂直保持视觉分隔，禁止向下偏移遮挡头像；
- 手牌下方牌型文字 + 彩色胜率进度条：X 42%–55%，Y 81%–85%，紧贴手牌下沿；
- 60s 倒计时环放在手牌左侧(如 X≈38%)，不遮牌。

## 三、桌面固定元素（保持不改，仅确认坐标）
- 荷官：X:50% Y:10%
- 底池：X:50% Y:32%
- 庄位 D + 红牌背装饰：X:28% Y:33%
- 公共牌：X 33%–67%，Y 38%–48%，横向平铺

## 四、底部操作按钮（不改）
四个按钮横向：整体 X 38%–98%，Y 88%–96%，顺序：弃牌 / 过牌|弃牌 / 过牌 / 跟任意注。保持现有实现即可。

## 五、禁止修改
1. 页面旋转逻辑不动（**并且移除上面提到的强制旋转 CSS**，回到用户手动横屏）；
2. 左侧竖向工具栏（返回/聊天/靶心/BUFF）坐标样式不改；
3. Vercel KV、房间权限、超时弃牌、筹码规则等后端逻辑不改；
4. 首页、房间页不改。

## 交付
- `npm run build` 通过；
- `git add -A && git commit -m 'ui: precise coord layout for seats + my hand, remove rotate hack' && git push origin main`；
- 简报改了哪些 CSS 值。
